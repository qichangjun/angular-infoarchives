// Generated by CoffeeScript 1.9.1
(function() {
  'use strict';
  angular.module("myApp").directive('hasPermission', [
    '$log', '$location', '$state', '$stateParams', '$document', '$timeout', '$window', 'permissions', function($log, $location, $state, $stateParams, $document, $timeout, $window, permissions) {
      var ctrlFun, directive;
      ctrlFun = function($scope, $element, $attrs) {
        var vm;
        vm = this;
        if (permissions.hasPermission($scope.permission)) {
          $element.show();
        } else {
          $element.hide();
        }
      };
      return directive = {
        restrict: 'AE',
        scope: {
          permission: '='
        },
        controller: ['$scope', '$element', '$attrs', ctrlFun],
        controllerAs: 'vm'
      };
    }
  ]);

}).call(this);

//# sourceMappingURL=hasPermission.directive.js.map
